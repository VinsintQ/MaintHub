spring.application.name=mainthub
server.port=8080
server.error.include-stacktrace=${SERVER_ERROR_INCLUDE_STACKTRACE:never}
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

spring.datasource.url=jdbc:postgresql://${DB_HOST:db}:${DB_PORT:5432}/${DB_NAME:repairflow}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.open-in-view=false

jwt-secret=${JWT_SECRET:C6UlILsE6GJwNqwCTkkvJj9O653yJUoteWMLfYyrc3vaGrrTOrJFAUD1wEBnnposzcQl}
jwt-expiration-ms=${JWT_EXPIRATION_MS:86400000}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
spring.mail.from=${MAIL_FROM:${MAIL_USERNAME:}}

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:doopsfdwr}
cloudinary.api-key=${CLOUDINARY_API_KEY:638859748674885}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:5p7XFXAWHdC9sxox14WByNbAVmE}
